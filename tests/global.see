int x;

int dupa(int &x, int y) {
    int z = y;
    return x;
}

int ee() {
    int x;
    // int x;
    int z = dupa(x, 3);

    {
        int x;
        int z = dupa(x, 3);
    }

    {
        int x;
        int z = dupa(x, 3);
    }

    {} {} {} {}
    if (true) {

    } else { }

    return z;
}

int y = 3;
